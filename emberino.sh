#!/bin/bash

sudo npm install -g grunt-cli

sudo npm install

sudo npm install -g bower

bower install

sudo npm install --save-dev grunt-contrib-coffee

sudo npm install --save-dev grunt-sass

sudo npm install --save-dev connect-livereload

grunt server

exit
